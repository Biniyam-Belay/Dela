# frontend/.env

# URL for making API calls (includes /api/v1 path)
VITE_API_BASE_URL=https://yofinance.onrender.com

# Base URL of the backend server (used for constructing image URLs)
VITE_BACKEND_URL=http://localhost:5000

# Optional: Stripe Publishable Key if using Stripe later
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_KEY
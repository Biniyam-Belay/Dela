# frontend/.env

# URL for making API calls (includes /api/v1 path)
VITE_API_BASE_URL=https://yofinance.onrender.com/api/v1

# Base URL of the backend server (used for constructing image URLs)
VITE_BACKEND_URL=https://yofinance.onrender.com